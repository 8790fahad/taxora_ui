VITE_API_URL=http://localhost:4000
# Optional. Public base path the app is served from. Leave unset (defaults to "/")
# for local dev, custom domains, and user pages. The GitHub Pages deploy workflow
# sets this automatically (e.g. "/taxora-ui/") for project pages.
# VITE_BASE=/
